[package]
authors = ["vyfor"]
categories = ["command-line-interface", "no-std", "no-std::no-alloc"]
description = "Minimal terminal spinners/throbbers for Rust"
edition = "2024"
keywords = ["terminal", "tui", "spinner", "throbber", "ratatui"]
license = "MIT OR Apache-2.0"
name = "rattles"
repository = "https://github.com/vyfor/rattles"
version = "0.3.1"

[lib]
crate-type = ["lib"]

[features]
default = ["std"]
std = []

[dev-dependencies]
crossterm = "0.29"
ratatui = "0.30"
